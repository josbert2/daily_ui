export const TextOverflow = (fullConfigTW) => {
    var dataPush = []
    
    var prefix = "truncate"
    dataPush.push(prefix)
    prefix = "overflow-ellipsis"
    dataPush.push(prefix)
    prefix = "overflow-clip"
    dataPush.push(prefix)

    return dataPush

}